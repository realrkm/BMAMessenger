package com.example.bmamessenger.ui.theme

import androidx.compose.ui.graphics.Color

// Defines the color palette for the application.

// The primary color, a deep purple.
val DeepPurple = Color(0xFF7F52FF)
// A lighter shade of purple, used as a secondary color.
val LightPurple = Color(0xFFA279FF)
// A dark blue, used for the background.
val NightBlue = Color(0xFF1A1A2E)
// A semi-transparent black, used for glassy surfaces.
val GlassyBlack = Color(0x80000000) // 50% transparent black
// A solid white color.
val White = Color.White
// A near-white surface color for light mode cards/inputs.
val LightSurface = Color(0xFFF4F5F8)
// A standard dark text color for light mode.
val ColorBlack = Color(0xFF111111)
